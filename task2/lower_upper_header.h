#ifndef LOWER_UPPER_HEADER_H
#define LOWER_UPPER_HEADER_H

#include <stdio.h>
#include <ctype.h>

int convert_to_lower(char * str, int length);
int convert_to_upper(char * str, int length);

#endif //LOWER_UPPER_HEADER_H

